# Example fixtures

Place small, non-licensed sample files here for tests and documentation.

- `sample_minimal.dbc` — optional minimal DBC for import tests (add when needed)
- Do **not** commit SAE, ISO, or OEM proprietary reference data

Private reference imports belong in `references/private/` (gitignored).
